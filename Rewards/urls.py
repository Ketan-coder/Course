from django.urls import path
from . import views

urlpatterns = [
    # path('', views.index, name='index'),
    # path('voucher/', views.VoucherListView.as_view(), name='voucher_list'),
    # path('voucher/<pk>/', views.VoucherDetailView.as_view(), name='voucher_detail'),
]
